version: '3'

services:
  uiowa-atr:
    build:
      context: .
    restart: always
    ports:
      - "1234:3000"
    environment:
      - NEXTAUTH_SECRET
      - NEXTAUTH_URL
      - DATABASE_URL
      - JWT_SECRET
      - NODE_ENV
